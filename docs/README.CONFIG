Configuration file format for NatAmbio (natambio)
================================================================================

natambio is configured by a single XML file passed on the command line. Lines
that should be ignored can be wrapped in standard XML comments (<!-- ... -->).
Tag content with leading/trailing whitespace is taken as-is.

This document describes the schema as accepted by the parser in src/naconf.cpp.
Tags not listed here are silently ignored. Several example configurations live
under the docs/config_samples/ directory.


Document structure
--------------------------------------------------------------------------------
The parser scans for the first <natambio> element either as the root element
or as a direct child of the root element. Everything outside <natambio> is
ignored. A typical wrapper looks like:

    <main>
      <natambio>
        ... blocks below ...
      </natambio>
    </main>

Inside <natambio> the following top-level blocks are recognised, in any order:

    <coeff>        one convolution coefficient set (from a file or derived
                   from the convolution of other coeffs)
    <xtc>         a XTC crosstalk-cancellation filter generator that
                   synthesises a pair of coeffs (direct + cross)
    <xtc_asym>     the asymmetric variant of <xtc>, for layouts where the two
                   speakers sit at different azimuths; synthesises three coeffs
                   (a shared direct + one cross per speaker)
    <low_and_high_filter>
                   a crossover filter generator that synthesises a
                   complementary low-pass / high-pass coeff pair
    <loudness>     an equal-loudness compensation filter generator that
                   synthesises a single minimum-phase coeff
    <convol>       one convolution routing channel
    <nae>     one PCA spatial-decomposition engine
    <jack_input>   JACK client name and input ports
    <jack_output>  JACK output ports
    <remote>       TCP port for the gain manager (optional)

<coeff>, <xtc>, <xtc_asym>, <low_and_high_filter>, <loudness>, <convol> and
<nae> may appear any number of times.
<jack_input> and <jack_output> should appear once each.
<remote> is optional and appears at most once.


Sample rate
--------------------------------------------------------------------------------
There is no global sample-rate tag. Before parsing the configuration, natambio
opens a minimal short-lived JACK client to read the running server's sample
rate, and uses it for everything:

  - the <xtc>, <xtc_asym>, <low_and_high_filter> and <loudness> blocks generate
    their filters at that rate (so they take no <sample_rate> tag), and
  - every <coeff> loaded from a <filename> is checked against it; a file whose
    sample rate differs aborts the program with an error.

JACK must therefore be running before natambio is started.


Gain convention
--------------------------------------------------------------------------------
All dB gain fields share one convention -- the `<gain>` fields (in <coeff>,
<convol>, <low_and_high_filter> and in the <port>s of <jack_input> and
<jack_output>) and the NAE gain fields (<front_gain>, <ambience_gain>,
<rear_gain>) are plain dB gains converted internally via `pow(10, dB/20)`:

    positive value  -> amplification (louder)
    negative value  -> attenuation (quieter)
    0               -> unity

For the NAE gain fields, omitting the value for the active mode is a parse
error (an unset gain cannot be told apart from 0 dB internally).


<coeff> -- convolution coefficient set
--------------------------------------------------------------------------------
A coeff holds one set of convolution coefficients. It is obtained in one of two
ways:

  (a) loaded from an audio file (<filename>), or
  (b) derived from the convolution of other coeffs (one or more <convol_coeff>).

Declaration order only matters between derived coeffs: one derived from another
derived coeff must appear after it (see (b) below). Everywhere else the order is
free, because the references are resolved once the whole file has been read: a
<convol> may name a <coeff> declared further down, and a derived coeff may
reference a file-loaded or generated coeff declared after it.

(a) File-loaded coeff
--------------------------------------------------------------------------------
Loads one channel from an audio file (any libsndfile-supported format).

    <coeff>
      <name>direct left</name>
      <filename>~/IR/direct_left.wav</filename>
      <channel>1</channel>
      <skip>0</skip>
      <length>8192</length>
      <gain>0.0</gain>
    </coeff>

    name         Unique identifier referenced by <convol>/<coeff_name>.       (required)
    filename     Path to the audio file. `~` expands to $HOME.                 (required unless
                                                                                <convol_coeff> is used)
    channel      1-based channel index to read.                                (optional, default 1)
    skip         Frames to skip at the start of the file.                      (optional, default 0)
    length       Frames to read. When omitted (or 0) the whole file is read    (optional, default:
                 and length is set to the file's total frame count, or that     whole file)
                 count minus <skip> when leading frames are skipped.
    gain         Per-coefficient gain in dB (+ amplifies, - attenuates).        (optional, default 0)

The file's sample rate must equal the JACK sample rate. natambio probes the
running JACK server's sample rate at start-up (before parsing) and checks every
loaded file against it; a mismatch aborts with an error.

(b) Derived (convolved) coeff
--------------------------------------------------------------------------------
When one or more <convol_coeff> tags are present, <filename> is ignored and the
coeff is built, after the whole config is parsed, by convolving the referenced
coeffs together (left to right):

    <coeff>
      <name>XTC EQ direct left</name>
      <convol_coeff>XTC direct</convol_coeff>   <!-- convolved with... -->
      <convol_coeff>DRC EQ left</convol_coeff>   <!-- ...this one        -->
      <length>16384</length>
    </coeff>

    name         Unique identifier referenced by <convol>/<coeff_name>.       (required)
    convol_coeff Name of another <coeff> to convolve. Repeatable (1..n).      (>= 1)
    length       Output length in samples. The convolution result is          (optional, default:
                 truncated (or zero-padded) to this length; 0/omitted keeps    full convolution
                 the full linear-convolution length.                           length)
    gain         Extra gain in dB applied to the result                       (optional, default 0)
                 (+ amplifies, - attenuates).

With a single <convol_coeff> the referenced coeff is copied (truncated/padded to
<length>); with several they are convolved in sequence. The convolution is a
double-precision FFT convolution (the fft_convolve_truncate routine in lib/dsp.c);
the coeffs, stored as float, are converted to double on the way in and back to
float when the result is written.

When <length> is omitted the derived coeff is set to the full linear-convolution
length, i.e. the sum of the referenced coeffs' lengths minus (count - 1). This
is computed while parsing when every referenced coeff is already declared;
references to coeffs generated later (<xtc>, <xtc_asym>, <low_and_high_filter>,
<loudness>)
are instead resolved when the convolutions are built.

All referenced coeffs must share the same sample rate; the derived coeff
inherits it. As with file-loaded coeffs, that sample rate must equal the JACK
sample rate.

Referenced coeffs may themselves be derived coeffs, and only then does the
declaration order matter: the derived coeffs are built in a single pass over the
coeff list, in document order, so a derived coeff referenced before it has been
declared still has no data, and the parse fails with an error. References to
file-loaded coeffs (<filename>) and to generated ones (<xtc>, <xtc_asym>,
<low_and_high_filter>, <loudness>) work in any order, because all of those
already carry their samples by the time the convolutions are built.

Special name: a <convol> that references `<coeff_name>delta</coeff_name>`
activates bypass mode (no convolution; only the convol's delay and
gain are applied to the mixed input).


<xtc> -- XTC crosstalk-cancellation filter generator
--------------------------------------------------------------------------------
A <xtc> block synthesises, from scratch, a pair of FIR filters implementing
the XTC (crosstalk cancellation) recursion combined with
a log-empirical ILD model fitted to public HRTF datasets (HUTUBS, RIEC, CIPIC,
BiLi, ARI). It produces two coeffs -- a direct-path filter and a
cross-path filter -- that are added to the coeff list under the names given by
<direct_filter_name> / <cross_filter_name>, exactly as if they had been loaded
from a file. They can then be referenced by <convol>/<coeff_name> (see the
four <convol> blocks in docs/config_samples/convol_drc_xtc.xml) or even by a derived
coeff's <convol_coeff>.

    <xtc>
      <itd_us>180</itd_us>
      <ild_db>10</ild_db>
      <ild_alpha>1.8</ild_alpha>
      <azimuth_deg>20</azimuth_deg>
      <length>4096</length>
      <direct_filter_name>XTC direct</direct_filter_name>
      <cross_filter_name>XTC cross</cross_filter_name>
    </xtc>

    itd_us              Inter-aural time difference, microseconds. Converted   (required)
                        to the XTC recursion delay using the JACK sample rate.
    ild_db              Inter-aural level difference per recursion step, in dB  (required)
                        of attenuation.
    ild_alpha           Log-empirical ILD model scale factor.                  (required)
    azimuth_deg         Source azimuth, degrees, fed to the ILD model.         (required)
    length              Length of each generated filter, in samples.           (required, > 64)
    direct_filter_name  Name of the resulting direct-path coeff.               (required)
    cross_filter_name   Name of the resulting cross-path coeff.                (required)

Every <xtc> parameter is mandatory; omitting one is a parse error.

The recursion always runs at the exact, unrounded ITD. It used to round it to
whole samples, because a tap was a write at an array index: at 48 kHz an itd_us
of 180 is 8.64 samples and became 9. An ITD error dt leaves a residual
2*sin(pi*f*dt) relative to the cancelling signal, which capped the design's own
crosstalk suppression in the top octaves -- around -27 dB above 8 kHz for the
usual values, where the exact ITD reaches the ladder's floor some 55 dB lower.
The recursion now runs in the frequency domain, where a tap is a linear-phase
factor and no rounding is involved; nothing else in the pipeline changes.

A fractional shift has a two-sided impulse response, so the design carries a
bulk delay common to both filters, which is what keeps that response from being
clipped at n = 0. XTC depends only on the delay BETWEEN the two filters, so a
delay common to both costs latency and nothing else.

That delay is a DURATION: 1.33 ms, which is 64 samples at 48 kHz and is scaled
to whatever rate JACK reports -- 59 samples at 44.1 kHz, 128 at 96 kHz. It is
scaled rather than fixed because what has to be compensated for elsewhere is
the time: a subwoofer fed on a path that bypasses XTC needs those samples added
to the <delay> of the <convol> blocks feeding it, and with a fixed tap count
that compensation would have to be recomputed for every sample rate instead of
being one figure in milliseconds. The configuration report prints the value and
its duration, so there is no arithmetic to do. <length> must exceed it, which
is checked and reported with the rate it was computed at.

None of this is configurable: the frac_delay and model_delay tags earlier
versions accepted are gone, and a configuration still carrying one is warned
that it is ignored. See tools/xtc_filters/README.md and the measurements in
tools/python_xtc_filters/compare_frac_delay.py -- note that the offline tools
keep their own explicit model_delay, which they do not scale.

The two generated coeffs are created at the JACK sample rate (see "Sample rate"
above; there is no <sample_rate> tag). The filter names must not collide with
any other coeff name. XTC coeffs are generated after all <coeff> blocks have
been read, so a derived coeff may convolve them via <convol_coeff>.


<xtc_asym> -- asymmetric XTC filter generator
--------------------------------------------------------------------------------
Same generator as <xtc>, for rooms where the two speakers are NOT at the same
azimuth from the listening position. The geometry is given twice, one <left>
and one <right> sub-block, and the block produces THREE coeffs: a direct-path
filter shared by both channels plus one cross-path filter per speaker.

The direct filter is shared because it depends only on the product of the two
crossed paths, so the asymmetry cancels out of it; what differs between the two
channels is the cross filter. Each cross filter carries the OTHER speaker's
ITD, since it is the one cancelling that speaker's crosstalk: cross_left, which
feeds the left speaker, leads with the right side's delay.

    <xtc_asym>
      <left>
        <itd_us>180</itd_us>
        <ild_db>10</ild_db>
        <ild_alpha>1.8</ild_alpha>
        <azimuth_deg>20</azimuth_deg>
      </left>
      <right>
        <itd_us>140</itd_us>
        <ild_db>8</ild_db>
        <ild_alpha>1.8</ild_alpha>
        <azimuth_deg>15</azimuth_deg>
      </right>
      <length>4096</length>
      <direct_filter_name>XTC direct</direct_filter_name>
      <cross_left_filter_name>XTC cross L</cross_left_filter_name>
      <cross_right_filter_name>XTC cross R</cross_right_filter_name>
    </xtc_asym>

    left, right             One sub-block per speaker, each holding that   (both required)
                            side's itd_us, ild_db, ild_alpha and
                            azimuth_deg, with the same meaning and the
                            same units as in <xtc>. All four are
                            required in each sub-block.
    length                  Length of each generated filter, in samples.   (required, > 64)
                            Common to all three.
    direct_filter_name      Name of the resulting direct-path coeff,       (required)
                            shared by both channels.
    cross_left_filter_name  Name of the cross coeff feeding the LEFT       (required)
                            speaker.
    cross_right_filter_name Name of the cross coeff feeding the RIGHT      (required)
                            speaker.

Every <xtc_asym> parameter is mandatory; omitting one is a parse error.

The exact ITD matters more here than in <xtc>: the integer path rounded each
side's itd_us independently, and the round-trip period itd_left + itd_right
inherited both errors, so a geometry could carry up to a full sample of period
error. The example above is one -- 180 us and 140 us at 48 kHz are 8.64 and
6.72 samples, which rounded to 9 and 7 gave a period of 16 instead of 15.36.
See the <xtc> section for what that cost, and for the 64-sample model delay the
design carries.

The routing is identical to the symmetric case -- the same four <convol> paths,
with two distinct cross coeff names instead of one repeated. A complete example
is in docs/config_samples/convol_drc_xtc_asym.xml.

Balance
.......
<xtc_asym> has no balance parameter. If the two channels do not arrive at the
same level at the listening position, trim them with the <gain> of the TWO
<convol> blocks that share the affected <to_output> -- both the direct and the
cross one, regardless of which input each takes.

Always ATTENUATE the channel that arrives louder (a negative <gain>) and leave
the other at 0 dB. The strict correction is a boost on the quieter channel, but
only the ratio between the two matters and attenuating spends no headroom.

To set it, play a mono signal through both channels and attenuate until the
image sits centred; that is accurate enough. Aim to get within about 1 dB,
because the balance is part of the cancellation and not a cosmetic trim: a
residual balance error b caps the achievable crosstalk cancellation at roughly
20*log10|1-b|.

    balance error       cancellation ceiling
    0.5 dB              about -25 dB
    1   dB              about -19 dB
    2   dB              about -14 dB
    3   dB              about -11 dB
    6   dB              about  -6 dB

If the DRC stage already levels both channels against a common target, the
balance is already handled and both gains stay at 0 dB.

An asymmetric layout is not expected to reach the performance of an equivalent
symmetric one; this block exists to make a compromised room usable. The model
is developed in docs/xtc/xtc_no_simetrico_es.md.


<low_and_high_filter> -- complementary crossover filter generator
--------------------------------------------------------------------------------
A <low_and_high_filter> block synthesises, from scratch, a complementary pair of
FIR filters that split a signal at a crossover frequency: a low-pass and a
high-pass. It produces two coeffs added to the coeff list under the names given
by <low_pass_coeff_name> / <high_pass_coeff_name>, exactly as if they had been
loaded from a file. They can then be referenced by <convol>/<coeff_name> (see
the four <convol> blocks in docs/config_samples/sub_monitors_convol_dipole.xml --
a typical subwoofer / satellite split) or by a derived coeff's <convol_coeff>.

    <low_and_high_filter>
      <frequency>100.0</frequency>
      <dB_octave>24</dB_octave>
      <gain>0</gain>
      <length>8192</length>
      <low_pass_coeff_name>Low pass FIR</low_pass_coeff_name>
      <high_pass_coeff_name>High pass FIR</high_pass_coeff_name>
    </low_and_high_filter>

    frequency             Crossover (cut-off) frequency, Hz.                 (required, > 0)
    dB_octave             Low-pass roll-off slope above the crossover, in    (required)
                          dB per octave.
    gain                  Pass-band gain applied to both filters, dB         (optional, default 0)
                          (+ amplifies, - attenuates).
    length                Length of each generated filter, in samples.       (required, > 0)
    low_pass_coeff_name   Name of the resulting low-pass coeff.              (required)
    high_pass_coeff_name  Name of the resulting high-pass coeff.             (required)

All <low_and_high_filter> parameters are mandatory except <gain> (optional,
default 0 dB); omitting any other is a parse error.

The low-pass is designed with firwin2 (a flat pass-band at <gain> dB up to
<frequency>, then a constant <dB_octave> dB/octave roll-off). The high-pass is
its exact complement -- a gain-adjusted linear-phase delta minus the
low-pass -- so that the two filters sum back to a gain-adjusted delta. Both are
then converted to minimum phase, and those minimum-phase filters are what get
applied.

The two generated coeffs are created at the JACK sample rate (see "Sample rate"
above; there is no <sample_rate> tag). The filter names must not collide with
any other coeff name. They are generated after all <coeff> blocks have been
read, so a derived coeff may convolve them via <convol_coeff>.


<loudness> -- equal-loudness compensation filter generator
--------------------------------------------------------------------------------
A <loudness> block synthesises a single FIR filter that follows an equal-loudness
(isophonic) contour. It produces one coeff -- added to the coeff list under the
name given by <filter_name> -- whose magnitude is the DIFFERENCE between the
chosen model's contour at <phon> and its contour at <ref_phon> (each contour
normalised to 0 dB at 1 kHz). The result is a "playback at <phon>, mastered for
<ref_phon>" loudness compensation curve: it is flat (0 dB) at 1 kHz and boosts
the low (and high) frequencies when <phon> is below <ref_phon>. The coeff is
minimum phase.

    <loudness>
      <phon>70</phon>
      <ref_phon>90</ref_phon>
      <model>fletcher-munson</model>
      <length>4096</length>
      <filter_name>loudness filter</filter_name>
    </loudness>

    phon         Target listening level, phon.                              (required)
    ref_phon     Reference (mastering) level, phon; subtracted from the     (required)
                 target contour.
    model        Equal-loudness model. One of:                              (required)
                   iso226-2003, iso226-2023  (analytic ISO 226 formula)
                   fletcher-munson           (tabulated 1933 curves)
                   a-weighting, b-weighting, c-weighting
                                             (IEC weightings; these ignore
                                              phon, so the difference curve is
                                              flat and the filter is a unit
                                              impulse)
                 (robinson-dadson is not implemented.)
    length       Length of the generated filter, in samples.                (required, > 0)
    filter_name  Name of the resulting coeff.                               (required)

The contour difference is sampled by firwin2 (a flat-at-1 kHz target curve,
log-interpolated/extrapolated from the model's tabulated points, rolling off to
silence outside 10 Hz - 20 kHz) and the resulting linear-phase filter is
converted to minimum phase. The generated coeff is created at the JACK sample
rate (see "Sample rate" above; there is no <sample_rate> tag). Its name must not
collide with any other coeff name. It is generated after all <coeff> blocks have
been read, so a derived coeff may convolve it via <convol_coeff> (see
docs/config_samples/nae_dipole_loudness_drc.xml, which convolves it with a DRC EQ
filter).


<jack_input> -- JACK client name and input ports
--------------------------------------------------------------------------------
    <jack_input>
      <clientname>natambio</clientname>
      <port>
        <name>input_left</name>
        <destname>system:capture_1</destname>
        <gain>-3.0</gain>
      </port>
      <port>
        <name>input_right</name>
        <destname>system:capture_2</destname>
        <gain>-3.0</gain>
      </port>
    </jack_input>

    clientname   JACK client name. Default: "natambio".                        (optional, one tag)
    port         JACK input port definition. May appear multiple times.        (>= 1 required)
      name       Port name (as seen on the JACK graph).                        (required)
      destname   External port to auto-connect on startup.                     (optional)
      gain       Input gain in dB. Default: 0 (unity).                         (optional)

The <gain> is applied to what arrives at the port, before anything downstream
sees it: every <convol> and <nae> fed by that port gets the scaled signal. It
follows the gain convention above (positive amplifies, negative attenuates), and
it is applied to a private copy -- the buffer JACK shares with the other clients
reading the same source port is never touched.


<jack_output> -- JACK output ports
--------------------------------------------------------------------------------
    <jack_output>
      <port>
        <name>output_left</name>
        <destname>system:playback_1</destname>
        <gain>-6.0</gain>
      </port>
      ...
    </jack_output>

    port         JACK output port definition. May appear multiple times.       (>= 1 required)
      name       Port name.                                                    (required)
      destname   External port to auto-connect on startup.                     (optional)
      gain       Output gain in dB. Default: 0 (unity).                        (optional)

The <gain> is the last thing applied to the port: everything summed into it --
the <convol> outputs and the <nae> outputs -- is scaled by it on the way out. It
follows the gain convention above, and it is a per-port trim, so it is the place
to match levels between two amplifiers or to pull a subwoofer down without
rebuilding its coeffs. It does not disturb the start/stop fade, which keeps
running underneath it.


<remote> -- TCP port for the gain manager
--------------------------------------------------------------------------------
    <remote>7000</remote>

The whole content of the tag is the TCP port to listen on. Without the tag no
socket is opened: there is no default port, so a configuration that says nothing
about remote control gets none.

The manager changes the <gain> of JACK ports and the parameters of the <nae>
engines while natambio runs. One command per line:

    up   <dB> <port> [port ...]      raise those ports' gains by <dB>
    down <dB> <port> [port ...]      lower them by <dB>
    upin    <dB>                     raise every input port by <dB>
    downin  <dB>                     lower every input port by <dB>
    upout   <dB>                     raise every output port by <dB>
    downout <dB>                     lower every output port by <dB>
    get  [port ...]                  report the gains as they are now
    naegain [<nae> [front|amb|rear <dB> ...]]   set or report an NAE's gains
    naepan  [<nae> [<scale>]]        the width of an NAE's input pair
    naeget  [<nae>]                  an NAE's configuration, as the XML block
    getxmlconfig                     the whole live configuration, as XML
    timecycle [reset]                xruns, and where the period goes
    mute                             silence every output, gains untouched
    unmute                           bring them back
    toggle                           whichever of the two is not the case now

    echo "up -1.0 front_output_left front_output_right" | nc -q0 localhost 7000

The number is signed and follows the gain convention above, so "up -1.0" and
"down 1.0" ask for the same thing; "down" is there so that a script computing a
positive step reads the way it means. Both are RELATIVE to the gain the port has
at that moment, which is what makes them usable from a key binding: send the
same line twice and the port ends up 2 dB away.

The names are JACK port names as declared in <jack_input> and <jack_output>, and
input and output ports may be mixed in one line. The result is clamped to
[-120, +20] dB -- with relative commands nothing else stops a key held down from
walking a gain up to where it damages a driver.

The four grouped forms take a number and nothing else -- naming a port after
one is refused rather than ignored, since a caller who writes "upout 1 sub" is
asking for something they are not getting. They are the two lists worth having
a name for: every output is the volume of the system, every input the level it
is fed at. Being relative like the rest, they move the balance bodily and leave
it as it was:

    $ echo "downout 3" | nc -q0 localhost 7000
    ok front_output_left -3.000
    ok front_output_right -3.000
    ok sub_output_left -3.000
    ok sub_output_right -3.000
    ok rear_output_left 3.000       <- was +6, and is still 6 dB above the front
    ok rear_output_right 3.000

"mute", "unmute" and "toggle" take nothing at all. Mute takes every output to
-120 dB whatever its own gain, and leaves those gains alone: they are what the
ports go back to on unmute, and what "get" keeps reporting meanwhile, so up/down
and the grouped forms go on working through a mute and take effect when it is
lifted. "toggle" is whichever of the two is not the case now, for a caller with
one button and no room for two.
Both directions are faded at the same rate as any other gain change -- a few
tens of milliseconds -- because cutting or restoring a signal mid-waveform is a
discontinuity the size of whatever sample was playing, which is a click either
way round.

The flag applies to the OUTPUT ports, which is where the sound stops. The inputs
are deliberately left running: silencing them too would add nothing audible and
would drain the convolution tails and the NAE's window into silence, so that
unmuting would rebuild them over a filter length instead of coming straight
back.

"get" takes no number and changes nothing; with no names at all it reports every
port, inputs first and then outputs, each in the order it was declared. That is
the other half of a session spent finding a balance by ear: the trims are made
with up/down and then read back, as they are the only record of what was found.
While mute is on it says so on a line of its own before the gains, which are
then what the ports will go back to rather than what is coming out:

    muted
    ok front_output_left -1.000

A caller that filters on "ok " skips that line without being taught to.

    $ echo "get sub_output_left rear_output_left" | nc -q0 localhost 7000
    ok sub_output_left -2.500
    ok rear_output_left 6.000

A whole balance is written out with a bare "get", and put back on a later run by
turning it into commands, the relative form being exact from a fresh start:

    echo "get" | nc -q0 localhost 7000 | awk '$3 != "0.000" { print "up", $3, $2 }'

"naegain" and "naepan" are the two commands that do not address a JACK port.
They name an <nae> engine by its <name> and set, respectively, the three
component gains (<front_gain>, <ambience_gain> and <rear_gain>, written front,
amb and rear here) and the input width (<pan_scale>). Their numbers are
ABSOLUTE, unlike the port commands: these are a balance and a width, which are
set to a value rather than nudged from wherever they happen to be. An <nae>
<name> is free text and may hold spaces, so it is quoted, with a backslash
escaping the next character inside the quotes:

    echo 'naegain "front stereo" front -1.5 amb -4.0' | nc -q0 localhost 7000
    echo 'naepan "front stereo" -0.3' | nc -q0 localhost 7000

With no value they report instead of setting, and with no name at all they
report every engine. "naeget" reports one engine's whole configuration as the
<nae> block it would be written as. See the <remote> section of src/README.md
for the details -- clamping, slew, which gains a mode actually reads, and why
naepan refuses a value outside [-1, 1] rather than clamping it.

"getxmlconfig" answers with the ENTIRE configuration as it now stands, valid to
start another natambio with. It takes nothing and changes nothing:

    exec 3<>/dev/tcp/localhost/7000
    echo getxmlconfig >&3
    while read -t 0.2 -u 3 line; do echo "$line"; done > tuned.xml
    exec 3<&- 3>&-

It is valid because it *is* the file that started this one -- comments, layout
and all -- rather than something reconstructed from what was parsed out of it.
What keeps it current is that every command that changes anything writes the new
value back into the configuration as well as into the running audio: up/down and
the grouped forms into that port's <gain>, naegain into <front_gain> /
<ambience_gain> / <rear_gain>, naepan into <pan_scale>. There is one place
holding what the system is set to, and nothing to reconcile when someone asks
for it. A tag the file left out because it was at its default -- <gain> is 0 dB
when absent -- is added the moment it stops being the default.

That is what makes an evening spent finding a balance by ear reproducible: trim
by ear with the relative commands, then write the result out as a configuration
rather than as a list of gains to be replayed.

"timecycle" is the one command that reports on natambio itself rather than on
what it is set to: how many periods were lost, and how long each stage of the
ones that were not actually takes. natambio counts every xrun JACK reports and
times every cycle of the JACK callback, of the convolution stage inside it --
the input sums, the convolution proper and the delay/scale/mix on the way out --
and of each <nae> engine's block in its own thread, keeps the last thousand of
each in a FIFO, and reduces them on request:

    $ echo timecycle | nc -q0 localhost 7000
    #	period_us	frames	rate
    ok	21333.333	1024	48000
    #	xruns	since_reset	n	mean_us	sd_us	min_us	max_us
    ok	23	9	9	1452.000	31.400	1401.000	1498.000
    #	stage	name	cycles	n	mean_us	sd_us	min_us	max_us	load_pct
    ok	total	-	1086	1000	358.707	68.190	114.968	725.522	1.681
    ok	conv	-	1086	1000	317.368	61.587	100.027	662.417	1.488
    ok	nae	"front stereo"	1086	1000	294.079	60.451	78.011	490.547	1.378
    ok	nae	""	1086	1000	254.470	50.352	78.679	468.486	1.193

Three tables, tab-separated, each under a header line naming its columns: seven
numbers in a row are unreadable without one, and nobody remembers which of them
is the deviation. The header lines begin with "#" and the data lines with "ok",
so a caller skips the headers the way it already skips the "muted" line of a
"get", and reads the rest with awk -F'\t'. Piped through column the same answer
lines up on a terminal:

    $ echo timecycle | nc -q0 localhost 7000 | column -t -s $'\t'
    #   period_us  frames          rate
    ok  21333.333  1024            48000
    #   xruns      since_reset     n       mean_us   sd_us    min_us    max_us
    ok  23         9               9       1452.000  31.400   1401.000  1498.000
    #   stage      name            cycles  n         mean_us  sd_us     min_us    max_us   load_pct
    ok  total      -               1086    1000      358.707  68.190    114.968   725.522  1.681
    ok  conv       -               1086    1000      317.368  61.587    100.027   662.417  1.488
    ok  nae        "front stereo"  1086    1000      294.079  60.451    78.011    490.547  1.378
    ok  nae        ""              1086    1000      254.470  50.352    78.679    468.486  1.193

The first table is the period itself: its length in microseconds, the frames it
holds and the sample rate. It is a table of its own because it is what the
machine is given rather than what it spends, and it comes first so that the
figures below have something to be read against without the caller having to
know the configuration.

The second is the xruns: the periods that did not fit at all, which is what the
load figures below are read in order to predict. Three counts, narrowing: every
xrun since natambio started, those since the last reset, and how many of them
the delay figures cover -- the same last-thousand FIFO as everything else here.
The delays are JACK's own figure for how late the period ran, in the same
microseconds, so the report says a machine dropped nine periods by about 1.45 ms
each and not merely that it dropped nine. There is no load column: an xrun is a
period that did not fit, and a percentage of the period it overran by would read
as a share of one.

Not every backend measures the delay. Those that do not report 0.0 for it, so a
row with a non-zero count and zeroes across the figures is that backend rather
than an absence of xruns -- the count is the reliable half of the row. The zero
is reported as the zero it was measured as rather than dropped, so the count and
<n> keep agreeing.

The third is one row per stage. <cycles> is every cycle timed since the last
reset and <n> how many of them the figures cover -- the same number until the
FIFO fills, and a thousand from then on, as above, where 1086 cycles have been
timed and the last 1000 reduced. The four times are microseconds; <load_pct> is
the mean as a percentage of the period, which is the number the question "is
this machine keeping up" is actually asking. A stage with no name of its own --
the callback and the convolution -- carries a "-" in that column rather than an
empty one, which between two tabs reads as a field that went missing. An engine
with no <name> is reported at its position in the file under an empty quoted
name, as it is the report where its absence would go unnoticed.

The cycle count matters most for the NAE rows. The callback signals the engines
once per period and does not wait for them, so an engine whose count trails the
callback's is one that is not being given the periods -- which shows in the
count and in no mean of the periods it did get.

    timecycle reset

empties every history and answers "ok timecycle reset". It is how a measurement
is made to start where the caller wants it to -- when the music starts, or after
a filter is swapped -- rather than where the last one left off.

The count of xruns since natambio started is the one figure it does not clear.
Every other number here is a window the caller is entitled to start where it
likes, but an xrun is an event and not a measurement: a caller resetting to time
the next minute is not asking to be told that the last hour was clean.

natambio also prints a line to stderr for every xrun, with the time of day on
it. That line and this count are the same events seen from two sides: the log is
for a person reading it afterwards, and tells them which track was playing; the
count is for a manager watching the control port while it happens, and is the
only one of the two a script can act on.

The counters are always on. They cost two clock reads per stage per period,
which is a few tens of nanoseconds against a period of a thousand microseconds
or more, and an xrun costs one increment and only when there is one to count: a
load figure that has to be switched on first is one nobody has when the xruns
start.

Every line is answered. One line per port on success, carrying the gain the port
has once the command is done -- the same shape for the port commands, and
"ok <nae> <which> <dB>" for naegain, so a caller parses one format either way:

    ok front_output_left -1.000
    ok front_output_right -1.000

or a single line beginning with "error:" -- unknown command, unparsable number,
a port that does not exist, a port named twice in an up/down, a port named at
all after one of the commands that take none. A command that
fails changes and reports NOTHING: half a trimmed speaker pair is worse than an
untrimmed one, and an answer is either complete or an error. A name repeated in
a "get" is merely answered twice, having nothing to double.

A change is not applied as a step. The callback slews to the new gain at the
same rate as the start/stop fade, a few milliseconds for a small trim, so a
level change during playback is heard as a fade and not as a click.

The socket is bound to 127.0.0.1 and the commands carry no credential of any
kind: anything that can reach the port owns the volume of the system. To drive
it from another machine, forward it over SSH

    ssh -L 7000:localhost:7000 user@host

which puts the authentication where it belongs. natambio refuses to start if the
port cannot be bound -- usually another instance already holding it, and going on
would leave this one running while the commands went to the other.


<nae> -- PCA spatial decomposition
--------------------------------------------------------------------------------
Each <nae> block defines ONE NAE engine. Use multiple <nae>
blocks for multiple engines.

    <nae>
      <name>front stereo</name>
      <mode>alpha</mode>
      <steps_length>5</steps_length>
      <front_gain>-2.0</front_gain>
      <ambience_gain>2.0</ambience_gain>
      <input_left>input_left</input_left>
      <input_right>input_right</input_right>
      <output_left>pn_output_left</output_left>
      <output_right>pn_output_right</output_right>
    </nae>

    name             Label for diagnostics.                                    (optional)
    mode             "alpha" (front dipole) or "beta" (rear dipole).           (required)
    steps_length     PCA analysis window length, in JACK buffer periods        (optional,
                     (the covariance spans steps_length blocks).                default 5)
    front_gain       Main-component gain, dB (positive = amplification;        (required for alpha)
                     see Gain convention). (alpha mode only)
    ambience_gain    Ambience-component gain, dB (positive = amplification).   (required for alpha)
                     (alpha mode only)
    rear_gain        Ambient-component gain, dB (positive = amplification).     (required for beta)
                     (beta mode only)
    pan_scale        Width of the input pair, [-1, 1].                        (optional,
                     A plain constant matrix on L/R, applied before anything     default 0)
                     else looks at the signal -- before the decomposition and
                     before the correlation the beta mode weighting rides on.
                     Written as an angle so the mid and side weights stay
                     power complementary: +1 collapses the pair to mono, 0
                     leaves it untouched, -1 puts the two channels in opposite
                     polarity. An exploratory control: it promises no
                     improvement in quality, and leaving it at 0 is a perfectly
                     good answer. See the notes below.
    input_left       Source JACK input port for the left channel.              (required)
    input_right      Source JACK input port for the right channel.             (required)
    output_left      Combined output (alpha: main + ambience; beta: the       (required, or
                     ambient component), written in BOTH modes: a          front/amb equivalent)
                     <jack_output>/<port>/<name>, or a virtual name
                     consumed by <convol>/<from_nae>.
    output_right     Right output (same resolution as output_left).            (required, or
                                                                                front/amb equivalent)
    front_output_left, front_output_right
                     Optional separate outputs for the main (front)
                     component on its own (alpha mode).                        (optional)
    amb_output_left, amb_output_right
                     Optional separate outputs for the ambience component
                     on its own (in beta mode they carry the ambient
                     signal); same name resolution as output_left/right.       (optional)

pan_scale, what to expect:

    It is there to be played with, not to make anything better. Nothing in it
    was put in to improve the engine: it changes the character of what the
    decomposition is fed, some settings will suit some material and some
    listeners, and none of that is an upgrade over leaving it alone. At 0 the
    output is bit identical to the untouched algorithm, so the cost of
    ignoring it is exactly nothing -- and if a listening session ends without
    finding anything worth keeping, that is a result and not a failure.

    It acts on the input, ahead of everything. The decomposition sees an
    already narrowed or widened pair, and so does the correlation the beta mode
    weighting rides on, so every later stage follows from what this leaves.

    It cannot preserve energy exactly, and no width control can: the matrix
    [[a,b],[b,a]] is orthogonal only for b = 0, which is the identity, or a = 0,
    which is a channel swap. Power complementary weights are as close as it
    gets, and that is what is used, wm^2 + ws^2 = 2 at every setting. Energy is
    then held exactly when mid and side carry the same power -- measured within
    0.02 dB across the whole range on decorrelated material -- and drifts when
    they do not, by about +2.8 dB at +1 on mid-heavy material and -10 dB on
    side-heavy material, with the mirror figures at -1.

Mode semantics:
    alpha (front dipole): writes main component to *output_left/right channels,
           ambience is mixed in via the ambience_gain.
    beta (rear dipole) : writes the ambient signal to *output_left/right channels
           using inter-channel correlation to distinguish centred from
           decorrelated content.

Port name resolution: when <output_left> / <output_right> / front_* / amb_*
match a <jack_output>/<port>/<name>, the signal is written directly to that
JACK output port. Otherwise the name acts as a virtual buffer that <convol>
blocks can pick up via <from_nae>.

See docs/config_samples/nae_alpha_beta_out.xml for a full example: a front engine
routed through its front/ambience outputs and a rear engine through plain
left/right outputs.


<convol> -- convolution routing channel
--------------------------------------------------------------------------------
Each <convol> defines one channel through the convolution engine. Inputs from
multiple sources are summed before convolution; outputs may fan out to several
sinks. There may be up to 64 <convol> blocks (zita-convolver hard limit).

    <convol>
      <name>direct left</name>
      <coeff_name>direct left</coeff_name>
      <from_input>input_left</from_input>
      <from_nae>pn_output_left</from_nae>
      <from_convol>some other convolver</from_convol>
      <to_output>output_left</to_output>
      <gain>-5.0</gain>
      <delay>0</delay>
    </convol>

    name           Unique identifier; targeted by other convolvers'           (required)
                   <from_convol>.
    coeff_name     Reference to a <coeff>/<name>, or "delta" for bypass.      (required)
    from_input     Mix in a <jack_input>/<port>/<name>. Repeatable.           (>= 0)
    from_nae  Mix in a NAE virtual output (left/right/front/amb).   (>= 0)
                   Repeatable.
    from_convol    Mix in another <convol>'s output by name. Repeatable.      (>= 0)
    to_output      Send convolver output to a <jack_output>/<port>/<name>.    (>= 0)
                   Repeatable. A convol with no <to_output> is intermediate-
                   only and feeds other convolvers via <from_convol>.
    gain           Output gain in dB (+ amplifies, - attenuates).             (optional, default 0)
    delay          Output delay in samples (sample-accurate ring buffer).      (optional, default 0)

Care must be taken not to create convolution loops with chained <from_convol>
references.


Example configurations
--------------------------------------------------------------------------------
Ready-to-use configurations for the current schema live under
docs/config_samples/, each illustrating one feature or a full system (the
file-loaded ones reference IR WAVs you must provide). See
docs/config_samples/README.md for the annotated catalogue, organised by use
case: convol-only stereo, NAE patch-panel (no convolution), stereo + NAE front
dipole, 2.1 with subwoofer crossover, split two-client chains, multiple parallel
output paths, A/B switching, and full two-dipole systems.


Notes
--------------------------------------------------------------------------------
 - All example files under docs/config_samples/ now use the current schema.
   The schema above reflects what the parser in src/naconf.cpp accepts; tags it
   does not recognise are silently ignored, which can mask configuration errors
   (so watch for typos in tag names).
 - An extended developer-oriented description of the program architecture
   (not the file format) lives in src/README.md.
